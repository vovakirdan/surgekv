pragma module;

pub tag Apply(string, proto.Command, Channel<string>);
pub tag Expire();

pub type Message = Apply(string, proto.Command, Channel<string>) | Expire();

@copy
pub type Handle = {
    requests: Channel<Message>,
};
