pragma module;

import server as server;

@entrypoint("argv")
fn main(port: uint) -> int {
    let serve_res = server.serve(port).await();
    return compare serve_res {
        Success(code) => code;
        Cancelled() => 1;
    };
}
